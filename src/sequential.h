#include <vector>

#ifndef __SEQUENTIAL_H_INCLUDED__
#define __SEQUENTIAL_H_INCLUDED__

void sequentialLDD(std::vector<std::vector<int> >& input_graph,
        std::vector<int>& cluster, double alpha, double beta);

#endif
